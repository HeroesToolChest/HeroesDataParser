using HeroesData.Loader.XmlGameData;

namespace HeroesData.Parser.XmlData
{
    public class EffectData
    {
        private readonly GameData GameData;

        public EffectData(GameData gameData)
        {
            GameData = gameData;
        }
    }
}
